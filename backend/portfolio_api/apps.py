"""Django app configuration for portfolio_api."""

from django.apps import AppConfig


class PortfolioApiConfig(AppConfig):
    """App config registering portfolio_api with Django's app registry."""

    name = "portfolio_api"
